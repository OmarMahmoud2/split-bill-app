app_identifier(ENV.fetch("APP_IDENTIFIER", "com.example.splitBillApp"))
apple_id(ENV.fetch("FASTLANE_APPLE_ID", "omarmali306@gmail.com"))

# Android specific configuration
json_key_file(ENV["SUPPLY_JSON_KEY"])
package_name(ENV.fetch("ANDROID_PACKAGE_NAME", "net.omarmali.splitapp"))
